# 任务：B 站主播桌面宠物（图片立绘 + 语音聊天）

## 1. 工程脚手架

- [x] 1.1 初始化 npm 工程（package.json、ESM、Node 20+），安装 electron、electron-builder、vite，`npm run dev` 启动空白窗口成功（验证：窗口出现、热重载生效）
- [x] 1.2 建立 `app/` 结构：`main/`（主进程）、`renderer/`（display/audio/chat/settings 模块）、`shared/`（协议与常量），入口就位（验证：dev 模式加载渲染页无控制台报错）
- [x] 1.3 配置 electron-builder：NSIS + 便携版 target、图标与产品信息（验证：`npm run dist` 产出两种交付物）

## 2. 桌面宠物壳

- [x] 2.1 透明无边框置顶窗口：无背景色块、角色可见（验证：桌面背景透过窗口空白区域可见）
- [x] 2.2 角色拖动与位置记忆：渲染端命中检测 + 主进程移动窗口，位置持久化（验证：拖到屏幕角落重启后仍在原位）
- [x] 2.3 区域外鼠标穿透：`setIgnoreMouseEvents` 动态切换 + 事件转发（验证：角色外双击桌面图标正常打开；角色上可拖动）
- [x] 2.4 系统托盘：显示/隐藏角色、打开设置、退出；托盘退出完全结束进程（验证：逐项操作生效）
- [x] 2.5 单实例锁（验证：二次启动唤起原窗口）

## 3. 素材管线（先行，阻断依赖）

- [x] 3.1 定义 manifest 格式与 `assets-app/<character>/` 目录规范，产出 `docs/asset-spec.md`（验证：文档覆盖立绘/方向集/眨眼/口型/主题/测试音）
- [x] 3.2 程序化生成占位素材包：2 套姿态立绘、方向集帧、眨眼 3 帧、口型 3 帧、日夜主题背景、测试音（验证：体积 < 5MB、结构完整）
- [x] 3.3 清单加载器与降级：缺字段提示、坏文件跳过、立绘回退占位（验证：删单帧眨眼图后启动提示且其余正常）

## 4. 角色呈现

- [x] 4.1 立绘渲染与姿态切换（验证：占位包姿态可切换）
- [x] 4.2 视线跟随：方向集选帧 + 微位移 + 2 秒回正（验证：鼠标绕行朝向连续变化）
- [x] 4.3 待机眨眼帧动画，随机间隔（验证：1 分钟多次眨眼且间隔不等）
- [x] 4.4 口型帧渲染接口：响度档位 → 闭/半/全帧（验证：测试音播放时口型同步开合）
- [x] 4.5 日夜主题切换与持久化（验证：切换重启保持）

## 5. 音频播放

- [x] 5.1 播放队列：依序播放、不重叠、停止并清空（验证：连续 3 条依序完整播放）
- [x] 5.2 音量控制与持久化（验证：播放中调节立即生效）
- [x] 5.3 AnalyserNode 响度分析接入口型档位，停止 300ms 内闭合（验证：目视 + 录屏抽帧）

## 6. 语音聊天

- [x] 6.1 设置页：三类服务配置（Base URL/Key/模型名），safeStorage 加密存储、Key 不回显（验证：重启配置保持，界面无 Key 明文）
- [x] 6.2 主进程请求代理与测试按钮：LLM/ASR/TTS 各发最小真实请求，失败按网络/鉴权/模型名分类提示（验证：真实 GLM 通过——LLM 连接成功 592ms，错 Key 正确提示鉴权失败；cogtts/glm-asr 成功路径待账号充值资源包）
- [ ] 6.3 录音与按键说话模式：按住录音、松开送识别（验证：说一句话返回识别文本）——已实现，待真实 API 验收
- [ ] 6.4 免提监听模式：VAD 开口/静默分段（验证：安静环境说话自动触发识别）——已实现，待真实 API 验收
- [x] 6.5 文字输入模式与模式切换持久化（验证：三种模式切换重启保持；未配置 ASR 时语音模式置灰引导设置）
- [ ] 6.6 对话链路：输入 → LLM（带上下文）→ 字幕展示 → TTS → 播放 + 口型（验证：语音提问后角色开口回答且字幕同步）——已实现，待真实 API 验收
- [x] 6.7 上下文多轮与清空（验证：真实 GLM 通过——告知"我叫小明"后追问正确回出"小明"；清空后同问回复"无法知道您的名字"）
- [x] 6.8 打断：播放中点击角色或新输入立即停止（验证：长回复播放中打断后接受新输入）

## 7. 集成验证与交付

- [ ] 7.1 端到端冒烟清单：全新目录运行便携版，覆盖启动、拖动、穿透、托盘、占位降级、完整语音链路（验证：清单逐项通过）
- [x] 7.2 撰写 README：构建方法、素材替换指引（指向 asset-spec.md）、API 配置说明（验证：按 README 从零构建成功）
