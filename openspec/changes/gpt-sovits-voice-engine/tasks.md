# 任务：GPT-SoVITS 语音引擎集成（分离式语音包）

## 1. 引擎包契约与文档

- [ ] 1.1 编写 `docs/voice-engine-spec.md`：引擎包目录布局、engine.json 字段契约（版本/Python 路径/启动参数/健康检查）、制作方法（基于 GPT-SoVITS 官方整合包裁剪推理项）（验证：文档可据此独立打出引擎包）
- [ ] 1.2 制作一个最小验证用引擎包（可用桩服务替代真实推理：同契约的 HTTP /ping + /tts 返回固定 WAV），用于开发期联调（验证：桩包按规范可被应用下载并启动）

## 2. 引擎下载与安装

- [ ] 2.1 设置页新增"语音引擎"区块：状态显示、下载源地址配置、下载/重新下载按钮（验证：区块渲染、状态随操作变化）
- [ ] 2.2 主进程下载器：流式下载到临时文件、进度事件推送设置页、完成后大小校验（验证：下载大文件进度更新、中断后可重下）
- [ ] 2.3 解压与安装：校验通过后解压到 `userData/voice-engine/`，读取 engine.json 校验契约，失败清理残留并提示（验证：桩包安装成功进入"已安装"态；坏包被拒并提示）
- [ ] 2.4 下载源地址持久化与默认值（委托人可后期填入正式地址）（验证：重启保持）

## 3. 引擎进程管理

- [ ] 3.1 EngineManager 状态机：absent/installed/starting/ready/error，状态事件推送设置页与语音流程（验证：各状态迁移可观测）
- [ ] 3.2 懒启动：首次合成请求触发 spawn（engine.json 声明的 Python 与参数），/ping 探活轮询至就绪（上限 90 秒）（验证：桩包启动后探活通过）
- [ ] 3.3 端口冲突顺延与持久化：默认 9880 被占则顺延并记录（验证：占用 9880 后引擎用新端口工作）
- [ ] 3.4 崩溃恢复：进程退出自动重启（≤3 次），超出进入 error 态并提示（验证：手动杀进程触发重启；连续 4 次进入错误态）
- [ ] 3.5 退出清理：应用退出时终止子进程树，不留孤儿（验证：退出后任务管理器无 python 残留）

## 4. 音色模型管理

- [ ] 4.1 音色目录与配置：`userData/voice-models/<name>/`（model/、ref/、voice.json），配置持久化（验证：导入后重启配置保持）
- [ ] 4.2 预训练模型导入：文件选择器选 gpt/sovits 权重复制入库并启用；缺文件/不配对给出指明原因的提示（验证：正常导入生效；缺 GPT 权重被拒）
- [ ] 4.3 参考音频上传：选择 3~10 秒 wav/mp3 + 参考文字，入库并持久化；可随时更换（验证：上传后配置生效，重启保持）
- [ ] 4.4 试听按钮：用当前音色合成固定文本并播放（验证：点击后能听到合成结果）

## 5. TTS 提供方扩展

- [ ] 5.1 TTS 设置增加 ttsType（openai / gptsovits）切换，默认 openai 向后兼容（验证：切换持久化，openai 路线行为不变）
- [ ] 5.2 主进程 gptsovits 合成分支：引擎就绪时按 api_v2 契约发合成请求（text/text_lang/ref_audio_path/prompt_text/prompt_lang/speed_factor），WAV 字节返回渲染端（验证：桩包下合成返回有效音频）
- [ ] 5.3 未就绪与错误分支：未安装/下载中/启动中/错误分别返回带原因的明确错误，不自动回退云端（验证：各状态下发起对话得到对应提示）
- [ ] 5.4 合成接入播放链路：返回音频进入现有队列，口型同步与打断行为与云端 TTS 一致（验证：合成播放时口型动、点击角色可打断）

## 6. 集成验证与交付

- [ ] 6.1 端到端验收：安装桩包 → 导入音色 → 合成试听 → 完整对话（文字回复 + 引擎语音 + 口型）→ 打断 → 退出无孤儿进程（验证：全流程一次性通过）
- [ ] 6.2 真实 GPT-SoVITS 引擎包联调：用真实推理包（委托人提供或按规范制作）替换桩包，验证启动、合成与首字延迟在可接受范围（验证：真实引擎出声，加载状态可见）
- [ ] 6.3 文档：README 增加"语音引擎"章节（下载来源配置、音色导入指引、硬件预期），CREDITS 增加 GPT-SoVITS 署名（验证：按文档可独立完成引擎接入）
